--- original_3.py+++ fixed_3.py@@ -1,8 +1,8 @@ counter = 0
 
 def increment_counter():
-    # BUG: Python thinks 'counter' is local because we assign to it,
-    # but we try to read it before assignment.
+    # Corrected: We need to use 'global' keyword to modify a global variable inside a function.
+    global counter
     print(f"Current counter: {counter}")
     counter += 1
 
