def factorial(n):
    # BUG: No base case (if n == 0 or n == 1: return 1)
    # This will cause RecursionError or hang indefinitely
    return n * factorial(n - 1)

print("Factorial of 5 is:", factorial(5))